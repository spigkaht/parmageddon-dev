# user enters postcode
# presses search button
# call to google places api with search query
# > return pubs in location (postcode)
# create OR find pub (by name?)
# save entries to Venues
# load entries from Venues
# load map and markers






        // sets request options for places query
        const request = {
          query: `Pubs in ${this.locationValue}`,
          fields: ['name', 'geometry'],
        };

        // run google places query. iterate results. output error if not OK
        var places = new google.maps.places.PlacesService(map);
        places.findPlaceFromQuery(request, function(results, status) {
          if (status === google.maps.places.PlacesServiceStatus.OK) {
            for (let i = 0; i < results.length ; i++) {
              console.log(results[i]);
              console.log("length: ", results.length);
            }
          }
          else {
            console.log("Error!");
          }
        })
